spring.data.mongodb.uri=mongodb+srv://anshulsiwach07_db_user:0eWdGAYVzKUMhrX8@cluster0.ldnkdpp.mongodb.net/cloudShare
spring.data.mongodb.database=cloudShare
spring.data.mongodb.auto-index-creation=true

server.servlet.context-path=/api

app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration

clerk.issuer=https://beloved-alien-72.clerk.accounts.dev
clerk.jwks-url=https://beloved-alien-72.clerk.accounts.dev/.well-known/jwks.json
clerk.webhook.secret=whsec_tlN5d1/Y35Wu62Tc4gdYyaa3SgHpIiAR


# AWS Credentials
aws.region=ap-south-1

# S3 Bucket
aws.s3.bucket=dropvault-anshul-s3


#RAZORPAY
razorpay.key.id=${RAZORPAY_KEY_ID}
razorpay.key.secret=${RAZORPAY_KEY_SECRET}

# MAIL (optional: disabled by default to avoid crashes without SMTP)
mail.enabled=${MAIL_ENABLED:false}
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:true}
spring.mail.from=${MAIL_FROM:no-reply@dropvault.local}